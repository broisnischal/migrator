{
  "name": "examples",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "start": "bun run index.ts",
    "dev": "bun run index.ts --watch",
    "generate": "bunx prisma generate",
    "migrate": "bunx prisma migrate dev --create-only",
    "migrate:up": "bunx prisma migrate dev",
    "pull": "bunx prisma db pull",
    "apply": "bunx prisma migrate deploy",
    "sync": "bunx prisma db push",
    "studio": "bunx prisma studio",
    "status": "prisma migrate status",
    "reset": "bunx prisma migrate reset"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "prisma": "6.3.1"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@prisma/client": "^6.3.1",
    "class-validator": "^0.14.1",
    "drizzle-orm": "^0.39.3",
    "drizzle-zod": "^0.7.0",
    "nestjs-zod": "^4.3.1",
    "zod": "^3.24.2"
  }
}
